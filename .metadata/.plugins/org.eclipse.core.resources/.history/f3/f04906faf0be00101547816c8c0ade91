package com.array.object;

import java.util.Arrays;

class Student{
	String stuName;
	String stuRollNo;
	String dept;
	Subject subDetails[];
	/*
	 * Subject subDetails[] = {
			new Subject("s101", 100, "si1"),
			new Subject("s102", 100, "si2"),
			new Subject("s103", 100, "si3"),
			new Subject("s104", 100, "si4")
	};
	 */
	subMark[] sub;
	public Student(String stuName, String stuRollNo, String dept, Subject[] subDetails, subMark[] sub) {
		this.stuName = stuName;
		this.stuRollNo = stuRollNo;
		this.dept = dept;
		this.subDetails = subDetails;
		this.sub = sub;
	}
	/*
	 * public void setSub(subMark[] sub) {
		this.sub = sub;
	}
	 */
	@Override
	public String toString() {
		return "Student [stuName=" + stuName + ", stuRollNo=" + stuRollNo + ", dept=" + dept + ", subDetails="
				+ Arrays.toString(subDetails) + ", sub=" + Arrays.toString(sub) + ", getClass()=" + getClass()
				+ ", hashCode()=" + hashCode() + ", toString()=" + super.toString() + "]";
	}
	
	
	
}
class Subject{
	String subName;
	int maxMarks;
	String subId;
	public Subject(String subName, int maxMarks, String subId) {
		this.subName = subName;
		this.maxMarks = maxMarks;
		this.subId = subId;
	}
	@Override
	public String toString() {
		return "Subject [subName=" + subName + ", maxMarks=" + maxMarks + ", subId=" + subId + ", getClass()="
				+ getClass() + ", hashCode()=" + hashCode() + ", toString()=" + super.toString() + "]";
	}
	
}
class subMark{
	Subject subject;
	int marks;
	public subMark(Subject subject, int marks) {
		super();
		this.subject = subject;
		this.marks = marks;
	}
	@Override
	public String toString() {
		return "subMark [subject=" + subject + ", marks=" + marks + "]";
	}
		
	
}
public class arrayOfObjects {
	public static void main(String args[]) {
		Subject subDetails[] = {
				new Subject("s101", 100, "si1"),
				new Subject("s102", 100, "si2"),
				new Subject("s103", 100, "si3"),
				new Subject("s104", 100, "si4")
		};
		subMark subMarks[] = {
				new subMark(subDetails[0], 89),
				new subMark(subDetails[1], 98),
				new subMark(subDetails[2], 78),
				new subMark(subDetails[3], 90)
		};
		Student s1 = new Student("Likhitha", "20121A05R5", "CSE", subDetails, subMarks);
		for(subMark sm : s1.sub) {
			System.out.println(sm);
		}
	}
}
